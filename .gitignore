# build output
dist/
.astro/

# Drop folders for source artwork — multi-MB exports.
# Leading slash matters: a bare "posters/" also matches public/posters/, which
# would silently exclude the optimised WebP the site actually serves.
/posters/
/rooms/

# dependencies
node_modules/

# vercel
.vercel/

# logs / env
*.log
.env
.env.*
!.env.example

# os
.DS_Store
Thumbs.db
